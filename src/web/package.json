{
  "name": "web",
  "version": "0.0.1",
  "description": "Minimal webpage for API calls",
  "main": "index.html",
  "scripts": {
    "build": "esbuild main.js --bundle --outfile=dist/bundle.js --minify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Noah Lehmann",
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "^9.22.0",
    "esbuild": "^0.25.1",
    "eslint": "^9.22.0",
    "globals": "^16.0.0"
  }
}
